import React from "react";
import ReactDOM from "react-dom/client";
import HiltonAIConcierge from "../hilton-ai-concierge.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HiltonAIConcierge />
  </React.StrictMode>
);
